# Programa para calcular el area de un cuadrado

lado= input("")
lado=int(lado)
#lado=float(lado)
#lado=str(lado) convierte a string
area= lado**2
#area = lado * lado
print("Esta es el area" + str(area)) # el str es porque no se pueden sumar string y numeros